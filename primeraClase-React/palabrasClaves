vite.config.js --No se toca
readme instrucciones como levaantar nuestra programa, como funciona de que se trata 
package sin este archivo no levanta, todo lo que esta en esta no se toca 
las dependecias son herramientas que nos sirven para el desarollo de la pag 
 siwempre asegurar q este en el RUN  y si no estas parado en el run de la aplicaion nos tira error
 nos movemos a la carpeta y ponemos npm install
 siempre tiene q estar el notnulls
 npm run dev 
NO TOCAR index.html (esta en la carpeta ReactSol)y NO TOCAR  main.jsx(esta en la carpeta src) 
crearRoot: crea un punto de entrada a la index
siempre LOS COMPONENTES ARRANCAN EN MAYUSCULA
se importan puntos de la entrada en la app
app.jsx
para q un componente pueda ser usada hay que exportarlo//export function App(){
function devuelve logica
return devuelve lo que vamos a ver
el estado tiene 3 partes: const(variable, setVariable) = useState(valor inicial );
const primero declaramos 
el estado se declara asi const[]
si esta en json hay q pasarlo a otro formato hay q hacer lo siguiente; 
 fetch('https://rickandmortyapi.com/api/character')
  .then((res) => res.json())//then es despues